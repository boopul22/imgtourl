import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'Bulk Image Upload - Multiple Photos to URLs | ImageURL',
  description: 'Upload multiple images simultaneously and generate URLs for all. Batch processing with progress tracking and export options for efficient image hosting.',
};
